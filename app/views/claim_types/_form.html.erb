<%= simple_form_for(@claim_type) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :name %>
    <%= f.input :display_name %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
